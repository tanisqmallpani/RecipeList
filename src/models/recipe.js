export class Recipe {
    constructor(
      id,
      name,
      ingredients,
      instructions,
    ) {
      this.id = null;
      this.name = name;
      this.ingredients = ingredients;
      this.instructions = instructions;
    }
  }